import {isPrepped} from "batcher/utils.js";

/* 

get list of all useable servers
    calculate the total number of 1.75gb ram slots
    ignore home
identify most profitable target
for each target
    schedule a HWGW batch that will fit in the available ram
    allocate each job as needed


*/

/** @param {import("..").NS} ns */
export async function main(ns) {
	
}

class ServerPool {
    constructor (ns) {
        
    }
}
